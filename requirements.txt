# Audio download
yt-dlp>=2024.1.0

# Audio analysis
librosa>=0.10.0
numpy>=1.24.0
soundfile>=0.12.0

# Audio manipulation
pydub>=0.25.0

# Time stretching (requires: brew install rubberband)
pyrubberband>=0.3.0

# Key / harmonic detection (heavier install — optional for Phase 0)
# essentia  # pip install essentia  (or essentia-tensorflow on Mac)

# Track identification (Phase 1)
# shazamio>=0.4.0
# pyacoustid>=1.3.0
